<!-- res/drawable/border_yellow.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Color de fondo transparente -->
    <stroke
        android:width="2dp"
        android:color="@android:color/holo_orange_light" /> <!-- Color del borde -->
    <corners android:radius="8dp" /> <!-- Esquinas redondeadas -->
</shape>
