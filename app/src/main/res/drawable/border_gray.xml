<!-- res/drawable/border_gray.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Fondo transparente -->
    <stroke
        android:width="2dp"
        android:color="@android:color/darker_gray" /> <!-- Color del borde gris -->
    <corners android:radius="8dp" /> <!-- Esquinas redondeadas -->
</shape>
